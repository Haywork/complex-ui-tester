/**
 * Twitter card image — re-exports the OG image so both social platforms
 * render the same 1200×630 dark-theme image without duplicating the JSX.
 */
export { default, alt, size, contentType } from "./opengraph-image";
