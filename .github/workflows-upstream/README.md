# Upstream workflows (disabled)

Các file YAML trong thư mục này là workflow gốc từ upstream repo, được giữ lại để tham khảo khi setup CI/CD cho fork.

GitHub Actions **không** chạy workflow ở đây — chỉ file trong `.github/workflows/` mới được kích hoạt.
